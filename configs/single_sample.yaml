data:
  # Default fastMRI single-coil sample available in this workspace.
  h5_path: "data/knee_singlecoil_val/file1000000.h5"
  slice_index: null

output:
  figure_dir: "outputs/figures"
  npz_dir: "outputs/npz"

reconstruction:
  use_root_sum_of_squares: true
